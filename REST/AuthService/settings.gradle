rootProject.name = project_name

def projects = [
        ':api'    : [name: "ru.forxy.auth.api", path: 'api'],
        ':client' : [name: "ru.forxy.auth.client", path: 'client'],
        ':service': [name: "ru.forxy.auth.service", path: 'service']
]

projects.each() { key, config ->
    include key
    def prj = project(key)
    prj.name = config.name
    prj.projectDir = file("$settingsDir/$config.path")
}